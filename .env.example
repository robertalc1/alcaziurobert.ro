# SMTP credentials for the /api/contact Vercel function (nodemailer).
# Copy to .env.local for local dev. Configure these as Vercel environment
# variables (Project Settings → Environment Variables) for production.

# SMTP server hostname (e.g. mail.alcaziurobert.ro, smtp.zoho.com)
SMTP_HOST=

# 587 (STARTTLS) or 465 (SSL). The function picks secure mode automatically when port is 465.
SMTP_PORT=587

# SMTP login (usually the full email address)
SMTP_USER=contact@alcaziurobert.ro

# SMTP password — NEVER commit this. Set as encrypted env var in Vercel.
SMTP_PASSWORD=

# "From" address shown to recipients. Defaults to SMTP_USER if unset.
SMTP_FROM=contact@alcaziurobert.ro

# Where contact form submissions are delivered.
CONTACT_TO=contact@alcaziurobert.ro
